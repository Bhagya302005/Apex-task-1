<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Final Capstone Web Application - Task 5" />
  <title>Task 5 | Final Project</title>

  <!-- Optimized Internal CSS -->
  <style>
    :root {
      --primary: #0f766e;
      --secondary: #134e4a;
      --light: #f0fdfa;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    body {
      background: var(--light);
      color: #333;
      line-height: 1.6;
    }

    header {
      background: var(--primary);
      color: #fff;
      padding: 1rem;
      text-align: center;
    }

    nav {
      display: flex;
      justify-content: center;
      gap: 20px;
      background: var(--secondary);
      padding: 0.5rem;
    }

    nav a {
      color: white;
      text-decoration: none;
      font-weight: bold;
    }

    nav a:hover {
      text-decoration: underline;
    }

    section {
      padding: 2rem;
      max-width: 1100px;
      margin: auto;
    }

    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1rem;
    }

    .card {
      background: white;
      padding: 1.5rem;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      transition: transform 0.3s;
    }

    .card:hover {
      transform: translateY(-5px);
    }

    button {
      background: var(--primary);
      color: white;
      border: none;
      padding: 10px 15px;
      border-radius: 5px;
      cursor: pointer;
      margin-top: 10px;
    }

    button:hover {
      background: var(--secondary);
    }

    img {
      max-width: 100%;
      border-radius: 10px;
      margin-top: 10px;
    }

    footer {
      background: var(--secondary);
      color: white;
      text-align: center;
      padding: 1rem;
      margin-top: 2rem;
    }

    /* Mobile Optimization */
    @media (max-width: 600px) {
      nav {
        flex-direction: column;
        align-items: center;
      }
    }
  </style>
</head>

<body>

<header>
  <h1>Final Capstone Web Application</h1>
  <p>Task 5 – Project & Optimization</p>
</header>

<nav>
  <a href="#about">About</a>
  <a href="#features">Features</a>
  <a href="#projects">Projects</a>
  <a href="#contact">Contact</a>
</nav>

<section id="about">
  <h2>About This Project</h2>
  <p>
    This is a fully responsive and optimized web application developed as a
    final capstone project using HTML, CSS, and JavaScript. It demonstrates
    performance optimization, cross-browser compatibility, and mobile
    responsiveness.
  </p>
</section>

<section id="features">
  <h2>Key Features</h2>
  <div class="cards">
    <div class="card">✔ Responsive Design</div>
    <div class="card">✔ Optimized CSS & JS</div>
    <div class="card">✔ Lazy Loading Images</div>
    <div class="card">✔ Cross-Browser Support</div>
  </div>
</section>

<section id="projects">
  <h2>Dynamic Project Section</h2>
  <p>Click the button to load projects dynamically.</p>
  <button onclick="loadProjects()">Load Projects</button>
  <div id="projectList" class="cards"></div>
</section>

<section id="contact">
  <h2>Contact</h2>
  <p>Email: example@email.com</p>
  <p>Phone: +91 XXXXX XXXXX</p>
</section>

<footer>
  <p>© 2026 Final Project | Task 5</p>
</footer>

<!-- Optimized JavaScript -->
<script>
  function loadProjects() {
    const projects = [
      "Portfolio Website",
      "Blog Application",
      "E-Commerce UI",
      "Dashboard App"
    ];

    const container = document.getElementById("projectList");
    container.innerHTML = "";

    projects.forEach(project => {
      const div = document.createElement("div");
      div.className = "card";
      div.textContent = project;
      container.appendChild(div);
    });
  }

  // Performance Optimization: Lazy Loading Example
  document.addEventListener("DOMContentLoaded", () => {
    const img = document.createElement("img");
    img.src = "https://via.placeholder.com/600x300";
    img.loading = "lazy";
    document.getElementById("about").appendChild(img);
  });
</script>

</body>
</html>
